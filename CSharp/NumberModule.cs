using System;

namespace CSharp
{
    public static class NumberModule
    {
        // TODO: NumberModule

        private static int Int(double number)
        {
            return (int) Math.Floor(number);
        }
    }
}